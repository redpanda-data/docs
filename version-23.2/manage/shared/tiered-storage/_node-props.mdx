import Link from '@docusaurus/Link';

export default function NodeProperties({name,frontmatter})  {
  return <Link to={frontmatter && frontmatter.linkRoot + 'reference/node-properties#' + name}><code>{frontmatter.deployment=='Kubernetes'? 'storage.tieredConfig.' + name : name}</code></Link>
}
