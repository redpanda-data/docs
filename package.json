{
  "name": "redpanda-docs-playbook",
  "version": "1.0.0",
  "description": "Redpanda documentation",
  "license": "ISC",
  "scripts": {
    "build": "antora --to-dir docs --fetch antora-playbook.yml",
    "start": "npm run build && wds --node-resolve --open index.html --watch --root-dir docs"
  },
  "dependencies": {
    "@antora/cli": "3.1.2",
    "@antora/site-generator": "3.1.2",
    "@asciidoctor/tabs": "^1.0.0-beta.4",
    "@octokit/plugin-retry": "^4.1.3",
    "@octokit/rest": "^19.0.7"
  },
  "devDependencies": {
    "@web/dev-server": "^0.2.1",
    "cross-env": "^7.0.3",
    "node-pandoc": "^0.3.0"
  }
}
