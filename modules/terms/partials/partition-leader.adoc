=== partition leader
:term-name: partition leader
:hover-text: Every Redpanda partition forms a Raft group with a single elected leader. This leader handles all reads and writes, and it replicates data to followers to ensure that a majority of brokers store the data. 