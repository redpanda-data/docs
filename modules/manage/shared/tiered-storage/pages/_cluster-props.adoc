import Link from '@docusaurus/Link';
:description: 

export default function ClusterProperties({name,frontmatter})  {
  return <Link to={frontmatter && frontmatter.linkRoot + 'reference/cluster-properties#' + name}>``{frontmatter.deployment=='Kubernetes'? 'storage.tieredConfig.' + name : name}``</Link>
}
