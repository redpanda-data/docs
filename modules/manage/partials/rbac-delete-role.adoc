To delete an existing role:

. From *Security* on the left navigation menu, select the *Roles* tab.

. Find the role you want to delete, then click the *...* (more options) menu at the end of its row.

. Click *Delete*.

. In the confirmation dialog, type the role name to enable the *Delete* button, then click *Delete*.