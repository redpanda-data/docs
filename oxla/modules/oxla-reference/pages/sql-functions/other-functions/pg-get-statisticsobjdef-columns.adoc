= pg_get_statisticsobjdef_columns()


## Overview

The <a href="https://www.postgresql.org/docs/current/functions-info.html#FUNCTIONS-INFO-CATALOG" target="_blank">pg_get_statisticsobjdef_columns()</a> is a system catalog information function that retrieves information about the columns associated with an extended statistics object.

## Syntax

The syntax for the `pg_get_statisticsobjdef_columns()` function is as follows:

<pre><code>pg_get_statisticsobjdef_columns() → NULL</code></pre>

## Restrictions

* This function always returns `NULL` if there are no parameters specified  
