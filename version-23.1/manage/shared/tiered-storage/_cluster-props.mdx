import Link from '@docusaurus/Link';

export default function ClusterProperties({name,frontmatter})  {
  return <Link to={frontmatter && frontmatter.linkRoot + 'reference/cluster-properties#' + name}><code>{frontmatter.deployment=='Kubernetes'? 'storage.tieredConfig.' + name : name}</code></Link>
}
